services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "7890:3000"
    volumes:
      - ./frontend/src:/app/src
      - ./frontend/public:/app/public
    depends_on:
      - backend
    networks:
      - app-net
    environment:
      - VITE_API_URL=http://localhost:7891

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "7891:8000"
    depends_on:
      - db-init
    networks:
      - app-net
    environment:
      - DB_HOST=xampp
      - DB_PORT=3306
      - DB_USER=root
      - DB_PASSWORD=
      - DB_NAME=makanantar

  xampp:
    image: tomsik68/xampp:8
    ports:
      - "8080:80"
      - "3306:3306"
    volumes:
      - xampp-data:/opt/lampp
    networks:
      - app-net

  db-init:
    image: tomsik68/xampp:8
    networks:
      - app-net
    depends_on:
      - xampp
    volumes:
      - ./backend/sql:/sql
    command: >
      sh -c "
        echo 'Waiting for xampp...' &&
        sleep 5 &&
        /opt/lampp/bin/mysql -h xampp -u root -e \"GRANT ALL PRIVILEGES ON *.* TO 'root'@'%' IDENTIFIED BY '' WITH GRANT OPTION; FLUSH PRIVILEGES;\" &&
        echo 'Grant done, running schema...' &&
        /opt/lampp/bin/mysql -h xampp -u root < /sql/schema.sql &&
        echo 'Schema done, running seed...' &&
        /opt/lampp/bin/mysql -h xampp -u root < /sql/seed.sql &&
        echo 'DB initialized successfully'
      "

networks:
  app-net:
    driver: bridge

volumes:
  xampp-data:
